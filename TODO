TODO

* Define a global header
* Implement a destructor for lists

Notes:
    backtrack_main_algorithm mostly works. It presents a weird bug where it inserts
    a -1 instead of a real value.
    Earliest debuggable instance with given text file is when i=7,j=3
